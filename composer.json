{
    "name": "passbolt/architecture",
    "description": "Pilot study for Passbolt architecture",
    "type": "project",
    "repositories": [
        {
            "type": "path",
            "url": "./plugins_ce"
        },
        {
            "type": "path",
            "url": "./plugins_ee"
        },
        {
            "type": "path",
            "url": "./plugins_cloud"
        }
    ],
    "require": {
        "passbolt/ce-autoload": "@dev",
        "passbolt/ee-autoload": "@dev",
        "passbolt/cloud-autoload": "@dev",
        "php": ">=7.3",
        "ramsey/uuid": "^4.2"
    },
    "scripts": {
        "ce": "COMPOSER=./plugins_ce/composer.json composer",
        "ee": "COMPOSER=./plugins_ee/composer.json composer",
        "cloud": "COMPOSER=./plugins_cloud/composer.json composer",
        "post-cloud": "cp ./plugins_cloud/composer.lock .",
        "refresh-ce": "git checkout ce && git checkout main src plugins_ce",
        "refresh-ee": "git checkout ee && git checkout main src plugins_ce plugins_ee",
        "refresh-cloud": "git checkout cloud && git checkout main src plugins_ce plugins_ee plugins_cloud",
        "merge-ce-in-ee": "git checkout ee && git merge ce",
        "merge-ee-in-cloud": "git checkout cloud && git merge ee",
        "merge-cloud-in-develop": "git checkout develop && git merge cloud"
    },
    "config": {
        "sort-packages": true
    }
}
